extends ../layouts/layout

block content
  .container
  h2= title
  a.nav-link(href="/admin/users/create") ➕ Criar novo utilizador
  table
    thead
      tr
        th Username
        th Email
        th Role
        th Ações
    tbody
      each u in users
        tr
          td= u.username
          td= u.email
          td= u.role
          td
            a.nav-link(href=`/admin/users/${u._id}`) Editar
            form.form.form(data-ajax method="post", action=`http://localhost:3001/api/admin/users/${u._id}`, style="display:inline")
              input.form-control(type="hidden", name="_method", value="DELETE")
              button.btn(type="submit") 🗑️
  p: a.nav-link(href="/admin") Voltar